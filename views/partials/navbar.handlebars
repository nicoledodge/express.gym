<nav class="navbar navbar-expand-lg navbar-light bg-light justify-content-center">
    <a class="navbar-brand" href="/">express.gym</a>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="navbar-collapse collapse">
        <ul class="navbar-nav mx-auto text-center justify-space-between">
            <li class="nav-item active">
                <a class="nav-link" href="/home">Home</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="/views/partials/amenities.handlebars">Amenities</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="/reservations">Class Schedule</a>
            </li>
        </ul>
    </div>
    {{#if logged_in}}
        <button class="no-button" id='logout'>Logout</button>
    {{else}}
        <a href="/login">Login</a>
    {{/if}}


</nav>
